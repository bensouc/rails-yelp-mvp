<div class="container col-6 bg-light border rounded p-2">
  <h1>Tell us what you think !!!</h1>
  <%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating,collection: [0,1,2,3,4,5] %>
  <%= f.submit %>
<% end %>
</div>
